[project]
name = "hackathon-project"
version = "0.1.0"
description = "FastAPI application on Vercel"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.vercel]
entrypoint = "company_research_complete:app"
